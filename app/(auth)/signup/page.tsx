import { SignupForm } from "@/components/signup-form";

export const metadata = {
  title: "Sign Up - BarberShop Admin",
  description: "Create an admin account for BarberShop",
};

export default function SignupPage() {
  return <SignupForm />;
}
